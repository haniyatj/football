// Renter Profile Page

import 'package:flutter/material.dart';

class RenterProfilePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Renter Profile'),
      ),
      body: Center(
        child: Text('Renter Profile Content Goes Here'),
      ),
    );
  }
}